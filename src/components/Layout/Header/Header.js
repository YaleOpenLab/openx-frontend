import React from "react";
import Toolbar from "../../Navigation/Toolbar/Toolbar";

const header = props => {
  return (
    <React.Fragment>
      <Toolbar />
    </React.Fragment>
  );
};

export default header;
